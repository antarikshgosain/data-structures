//Problem statement - given a string, generate all its possible permutations and combnations
//P(a) = [a]
//P(ab) = [ab,ba]
//P(abc) = [abc, acb, bac, bca, cab, cba]

var str = "abc";

function permutation(long, short){

    
}

permutation(str,"")